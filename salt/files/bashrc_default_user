alias de='cd ~/Desktop'
alias cr='su - root'
alias sourceit='source ~/.bashrc'

findit() {
  find . -type f | xargs grep $1
}

removetrailingwhitespace() {
  for file in $@; do
    sed -i 's/[ \t]*$//' "$file"
  done
}
